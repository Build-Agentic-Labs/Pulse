NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=

# Daily auth-mail canary (docs/runbooks/notifications.md §1). Resend's test sink
# always reports "delivered"; the account must exist in Supabase auth
# (node scripts/create-auth-mail-canary.mjs). Unset = canary off.
AUTH_MAIL_CANARY_EMAIL=delivered@resend.dev

name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Node 24 to match the development machine (npm 11): npm 10 and npm 11 compute
      # different lockfile trees for platform-excluded optional deps (sharp/@emnapi),
      # so CI must resolve with the same npm generation that writes the lockfile.
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      # next-env.d.ts is gitignored (generated by `next dev`/`next build`); recreate it
      # so `tsc --noEmit` sees the same ambient Next.js types it does locally.
      - name: Restore next-env.d.ts
        run: |
          printf '/// <reference types="next" />\n/// <reference types="next/image-types/global" />\n' > next-env.d.ts

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

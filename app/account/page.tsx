import { AccountWorkspacePage } from "@/components/account-workspace-page";

export const metadata = {
  title: "Account & Workspace | BuildLogic Line Planner",
  description: "Manage BuildLogic workspace access and projects.",
};

export default function AccountPage() {
  return <AccountWorkspacePage />;
}

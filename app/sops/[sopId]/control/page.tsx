import { SopControlClient } from "@/components/sop/sop-approval-panel";

export const metadata = {
  title: "SOP control | Pulse",
};

export default function SopControlPage() {
  return <SopControlClient />;
}
